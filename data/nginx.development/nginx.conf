server {
  listen 80;

  root /var/www/html;
  index index.html;

  location ~ /api/.+ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://backend:80;
  }

  location ~ / {
    proxy_pass http://frontend:3000;
  }
}
